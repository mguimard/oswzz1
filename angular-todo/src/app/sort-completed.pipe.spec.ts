import { SortCompletedPipe } from './sort-completed.pipe';

describe('SortCompletedPipe', () => {
  it('create an instance', () => {
    const pipe = new SortCompletedPipe();
    expect(pipe).toBeTruthy();
  });
});
